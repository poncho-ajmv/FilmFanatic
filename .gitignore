# dependencias
/node_modules

# producción
/build

# variables de entorno (contienen la API key — nunca subir al repo)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# cobertura de tests
/coverage

# carpeta temporal de archivos por borrar (contiene el .env viejo con la key)
/_to_delete

# sistema
.DS_Store
